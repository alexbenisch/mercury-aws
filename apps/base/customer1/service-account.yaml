# =============================================================================
# Bead 8.1: Customer1 Service Account with IRSA
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: customer1-sa
  namespace: customer1
  annotations:
    # This ARN will be replaced with actual value from Terraform output
    eks.amazonaws.com/role-arn: "${SECRETS_MANAGER_ROLE_ARN}"
  labels:
    app.kubernetes.io/name: customer1
    app.kubernetes.io/component: service-account
